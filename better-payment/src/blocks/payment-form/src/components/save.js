/**
 * Save component for the Payment Form block.
 *
 * This block is dynamic, so the save component returns null.
 * The actual content is rendered on the server using render.php.
 *
 * @package Better_Payment
 * @since 1.0.0
 */

/**
 * Save component.
 *
 * Returns null because this is a dynamic block that renders on the server.
 *
 * @returns {null} Returns null for dynamic blocks.
 */
export default function save() {
    return null;
}

